<% provide(:title, 'All users') %>
<h1>All users</h1>

<%= will_paginate %>

<ul class="users">   <%# This will call user class and then user partial for all the users %>
  <%= render @users %>
</ul>

<%= will_paginate %>